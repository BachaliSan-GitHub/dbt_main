version: 2

models:
  - name: int_customers_created_record_flag
    description: "Customer snapshot with active flag"

    columns:
      - name: customer_id
        tests:
          - not_null

      - name: created_record_flag
        tests:
          - accepted_values:
              arguments:
                values: ['Y', 'N']